import { Box, Heading } from "native-base";
const index = () => {
    return (
        <Box>
            <Heading textTransform={"uppercase"} fontSize="lg">
                Travel up
            </Heading>
        </Box>
    );
};

export default index;
