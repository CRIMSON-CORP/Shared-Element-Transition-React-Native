import { Box, Heading } from "native-base";
const index = () => {
    return (
        <Box>
            <Heading textTransform={"uppercase"} fontSize={20}>
                Travel up
            </Heading>
        </Box>
    );
};

export default index;
